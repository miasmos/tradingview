export enum Subdomain {
    News = "news-headlines",
    Scanner = "scanner",
    Middleware = "support-middleware",
    Root = "www"
}

export enum HeadlineCategory {
    Stock = "stock"
}

export enum Locale {
    English = "en"
}

export enum Market {
    America = "america"
}
